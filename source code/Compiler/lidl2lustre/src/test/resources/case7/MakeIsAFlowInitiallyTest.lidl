import lidl.lang.*;
interaction
	(My Simple User Interface(theConst: Number in)
(theResult: Number out)): Activation in
is
(
    (All
        ((theResult) = (y))
        ((y) = ((Previous(y))+(1)))
    )
    With Behaviour
    (Make (y) Is A Flow Initially (theConst))
)